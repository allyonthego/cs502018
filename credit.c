#include <cs50.h>
#include <stdio.h>

int main (void)
{
    //prompt user for card #
    long long ccn;
    do
    {
        ccn = get_long_long("Number: \n");
    }
    while (n >= 0);

    //multiply every other digit, starting from second last, by two


    return 0;
}